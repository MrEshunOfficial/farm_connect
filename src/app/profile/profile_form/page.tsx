// ProfileContainer.tsx - Main container component
"use client";
import React from "react";
import ProfileForm from "./ProfileForm";

const ProfileContainer: React.FC = () => {
  return <ProfileForm mode="create" />;
};

export default ProfileContainer;
